# Floating soundtrack player (src/components/spotify/FloatingSpotifyPlayer.tsx).
# A public playlist URI or share URL — no client secret or OAuth token needed,
# the player only uses Spotify's public embed IFrame API. Leave unset and the
# player shows a graceful "not configured yet" state instead of a broken embed.
NEXT_PUBLIC_SPOTIFY_PLAYLIST_URI=
